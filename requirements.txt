# Runtime: numpy alone (FFT in the exact reference distribution).
# Nothing in the package imports Qiskit -- see README, "Why there is no
# circuit module". Install it only if you are adding circuit work.
numpy
pytest>=7.0
